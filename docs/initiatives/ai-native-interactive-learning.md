# AI-Native Interactive Learning

> ## 🔒 Routine ownership — read this first
> This initiative is built **exclusively** by the `openshiksha-ai-features`
> scheduled routine. The `openshiksha-plan`, `openshiksha-execute`, and
> `openshiksha-dependabot` routines **MUST NOT** plan, select, or build any of
> its increments — even when it is the only thing with open work. It is
> intentionally **excluded from the priority table** in
> [`STATUS.md`](STATUS.md). The ai-features routine, in turn, works **only**
> from this backlog — not the general roadmap.

**Status:** 🟢 Active (promoted 2026-06-15) · **Owner:** `openshiksha-ai-features` routine

---

## North Star

Build, one iron-clad increment at a time, toward a **~2-minute "golden-path"
demo** of OpenShiksha where **every beat is (a) not in the market, (b) tactile
and verifiable on screen, and (c) iron-clad by construction.** The routine
literally *assembles the demo* over time (see [`docs/demo/golden-path.md`](../demo/golden-path.md)).

**Flagship capability (Phase 1):** *AI builds a working, auto-graded interactive
widget from a plain-English description.* A teacher types "a number line where
students mark ¾" and a validated, sandboxed, gradeable widget appears in the
live preview. Most ed-tech AI is a chatbot or a text question generator — AI
that emits a **validated interactive manipulative** is the differentiated,
"not-in-the-market" moment, and it's the *safest* AI pattern: the output is
schema-validated data, never code.

## Why this exists

OpenShiksha is pre-launch with no real usage, and its feature set is at parity.
The next value isn't more polish or more dashboards (those read as "air" — clever
but unfalsifiable). It's a small number of capabilities a viewer can watch *work
and be correct*. This initiative builds exactly those, on top of the two things
the platform already does well: **deterministic, sandboxed interactivity** (the
Interactive Widgets Framework) and a **bounded LLM layer** (the explanation/
hint/grading cascade).

## Principles — the iron-clad bar (every increment ships meeting all of these)

1. **AI lives on the backend/host, never in the sandbox.** The widget runtime
   stays deterministic and network-less by design; the host mediates any AI call.
2. **AI is never in the grading/correctness path.** Grades come from the existing
   per-subpart grader (or a deterministic check). AI only **authors** or **coaches**.
3. **Every AI output that becomes data is schema-validated before it reaches the
   runtime or DB** — invalid output is repaired or rejected, never rendered raw.
4. **Always a deterministic fallback.** No key / timeout / malformed output still
   yields a working result (a safe default config, a static hint), proven by a test.
5. **Grounded, not free-floating.** AI sees the real state (the kind's schema, the
   actual numbers, the student's reported value) so it can't hallucinate about
   what it can't see.
6. **Honest provenance.** Real LLM output wears the `✨ AI-generated` `AIBadge`;
   deterministic/stub output wears the neutral `Auto-…` badge. Stub is never
   shown as if it were a real generation.
7. **Demo-able + tested, in the same increment.** Each increment extends the
   golden-path script and ships a test that exercises the **real path and the
   fallback path** — never as a follow-up.

## Foundation — what already exists (reuse, don't reinvent)

- **Widgets framework** (`frontend_modern/src/widgets/`, [initiative](interactive-widgets-framework.md)):
  sandboxed `allow-scripts` iframe; registry kinds `thermo-piston`,
  `number-line`, `function-plotter`, `fraction-bar` (+ admin `custom-html`,
  built-in `studio-scene`); each kind has a `params.schema.json`;
  answer-producing widgets report through the protocol into the **per-subpart
  grader**; `WidgetGalleryPanel` (IW-5) already renders a **live sandbox
  preview** + a schema-driven config form inside `CreateQuestionPage`; croupier
  `{{var}}` substitution flows into `widget_config` per student.
- **Backend widget guard** (`apps/core/widgets.py`): `KNOWN_WIDGET_KINDS` +
  `validate_widget_config`. ⚠️ Today it enforces only the **floor** (kind must be
  known, config must be a dict) — **per-kind JSON-Schema validation is NOT yet
  server-side.** DTB-1 closes that, because the whole initiative's safety rests
  on it.
- **LLM layer** (`apps/ai/llm_client.py`): Claude → Gemini → Ollama → **stub**
  cascade, `model_used` on serializers, `resolve_ai_language` (en/hi). Endpoints
  use the `@action generate` + 202-poll async pattern; provenance via the shared
  `AIBadge`.

## Backlog — Phase 1: Describe-to-Build (PR-sized, lowest-risk-first)

| ID | Increment | Status |
|----|-----------|--------|
| DTB-1 | **Server-side per-kind schema validation** (the guardrail keystone). Bring each kind's JSON Schema to a canonical backend source; `validate_widget_config` enforces the kind's schema, not just the floor. Reject invalid configs with a clear 400. **No AI yet.** Tests: a valid + an invalid config per kind. | ✅ Done (#365) |
| DTB-2 | **`/ai/widget-authoring/` endpoint**: NL prompt + the known kinds/schemas → proposes `{widget_kind, widget_config}`, **always validated by DTB-1**; on invalid → bounded repair (clamp / one re-ask) → deterministic **stub default** for the best-guess kind. Returns `model_used`. Tests: real-mocked **and** stub both return a **schema-valid** config; malformed LLM output never escapes. | ✅ Done (#373) |
| DTB-3 | **"Describe a widget" UI** in `WidgetGalleryPanel` / `CreateQuestionPage`: a prompt box → calls the endpoint → renders the proposal in the **existing live sandbox preview** → teacher edits via the existing schema form → attach. `AIBadge`, loading/empty/error states, friendly fallback line. The on-screen wow. | ✅ Done (#380) |
| DTB-4 | **Demo golden-path beat + capture**: extend `docs/demo/golden-path.md` (Beat 2 text landed with DTB-3); **capture a screenshot/gif** on a dev-stack run; an e2e smoke that drives type → generate → render → (student) grade. First use of the "routine accumulates the demo" mechanic. | ✅ Done (#387) |
| DTB-5 | **Variable-aware generation (backend)**: with `allow_variables`, the AI may emit `{{var}}` bindings + validated `variable_constraints` so a generated widget is **per-student randomized** via croupier. Deterministic guardrail reconciles token↔constraint (drops unbacked tokens, validates ranges); typed-leaf substitution makes a numeric `{{var}}` land as a number. Reuses the existing croupier path; the grader is untouched. | ✅ Done (#415) |
| DTB-5b | **Surface randomization in the Describe-it UI**: an "each student gets different numbers" toggle on the prompt box that sets `allow_variables` and persists the returned `variable_constraints` on attach, so the wow is on-screen. | ✅ Done (#424) |

**Phase 1 DoD:** a teacher types a description → a schema-valid, sandboxed,
auto-graded widget renders and attaches — provably, with the stub path tested —
and the describe-it beat records cleanly into the golden path. ✅ **Met** —
DTB-1..5 + DTB-5b shipped: describe → (optionally per-student randomized) →
validated render → attach → deterministic grade, end to end, with the on-screen
randomization toggle closing the last increment.

## Backlog — Phase 2: Guided step-validator (PR-sized, lowest-risk-first)

A student solves an equation one line at a time; a **deterministic engine** checks
each line's algebraic equivalence to the line above; AI **only** explains a wrong
step. Correctness deterministic; AI coaches. The trustworthy-math beat.

| ID | Increment | Status |
|----|-----------|--------|
| GSV-1 | **Deterministic algebraic-equivalence engine** (the Phase 2 correctness keystone, the counterpart to DTB-1). `apps/core/algebra.py`: a safe recursive-descent parser (numbers, vars, `+ - * / ^`, unary minus, whitelisted funcs — re-using the `function-plotter` evaluator shape on the host, **no `eval`**) + **fixed-seed numeric probing** (no CAS / no `sympy`). `check_step(prev, cur)` auto-detects equation-vs-expression and returns a structured `EquivalenceResult`; malformed input is the deterministic fallback (verdict + `error`, never a 500). **No AI yet.** Tests: equivalent/inequivalent expressions & equations across curriculum forms + every malformed-input path + determinism. | ✅ Done (GSV-1) |
| GSV-2 | **Step-solver widget** (`step-solver`): a `function-plotter`-style sandboxed widget — student enters successive lines; **GSV-1 runs in-sandbox** per line (the iframe is network-less, so the live check is deterministic JS, not a backend round-trip) and shows ✓/✗ liveness. Answer-producing → reports the final line through the existing per-subpart grader (AI nowhere near the grade). Schema + parity-guarded vendored schema (DTB-1 path). **Sliced:** GSV-2a = the in-sandbox equivalence engine (done); GSV-2b = the widget UI + schema + answer-reporting. | ✅ Done — GSV-2a (#438) + GSV-2b (this run) |
| GSV-3 | **AI wrong-step explainer** (`/ai/step-hint/`): given a line GSV-1 has **already judged wrong** + the previous line, the LLM emits a short, grounded explanation of the slip. AI sees the real lines + the deterministic verdict; never decides correctness. `model_used` + `AIBadge`; no-key/timeout → a deterministic static hint, tested. | ✅ Done (this run) |
| GSV-4 | **Demo golden-path beat + capture**: the on-screen step-solver wow — type a wrong step, watch the ✗, read the AI explanation; e2e smoke + screenshot/gif. Extends `docs/demo/golden-path.md`. **Sliced:** GSV-4a = the host-side AI step-coach surface (`useStepHint` + `StepHintPanel`) consuming GSV-3, honest provenance + tested fallback, wired into the `/widgets/dev` playground (done); GSV-4b = the sandbox→host protocol message that auto-feeds the widget's wrong-step events to the coach inline + the real-browser e2e + screenshot/gif capture. | ✅ Done — GSV-4a (#…) + GSV-4b (this run) |

**Phase 2 DoD:** a student solves a line at a time; a **deterministic** engine
lights each line ✓/✗ in the sandbox; a wrong step auto-feeds a **host-side** AI
coach that explains the slip — correctness deterministic throughout, AI nowhere
near the grade. ✅ **Met** — GSV-1..4 shipped: in-sandbox + backend equivalence
engines (anti-drift tested), the `step-solver` widget reporting through the
existing grader, the `/ai/step-hint/` server-gated explainer, and the on-screen
coach that now **auto-targets the exact wrong step** the student just made
(Beat 10). Phase 3 (propose-and-verify) is the next phase.

## Backlog — Phase 3: Propose-and-verify practice bank (PR-sized, lowest-risk-first)

AI **proposes** a widget *problem* (config + the question's `correct_answer`); a
**deterministic engine confirms the problem is well-posed and the expected answer
is actually reachable** before anything ships. **AI proposes, the engine
disposes** — correctness deterministic throughout. Feeds SRS / difficulty
calibration later.

| ID | Increment | Status |
|----|-----------|--------|
| PV-1 | **Deterministic problem verifier** (the Phase-3 correctness keystone, the counterpart to DTB-1 / GSV-1). `apps/core/problem_verifier.py`: `verify_widget_problem(kind, config, correct_answer, variable_constraints=…)` → a structured `ProblemVerdict`. Confirms (1) config schema-valid (reuses DTB-1), (2) an answer is present, (3) the answer is **reachable on the widget** — for an answer-producing kind the value must be one the widget can actually emit. Faithfully ports the `number-line` `snap` (encoding the DTB-4 "¾ + step 0.25 → 0.8 is unreachable" bug class) and ties "reachable" to the grader's own tolerance. Variable-aware: samples the croupier exactly as grade-time will, over a deterministic batch of synthetic students, and rejects a problem unreachable for *any* of them. **No AI.** Never raises — malformed input is a deterministic `ok=False`. Tests: reachable/unreachable/out-of-range/non-numeric literals, every malformed path, explanatory-kind no-op, variable reachable-for-all vs unreachable-for-some, determinism. | ✅ Done (this run) |
| PV-2 | **`/ai/practice-problem/` endpoint**: NL topic + kind → AI proposes `{widget_config, correct_answer}`, **PV-1 verifies before returning**; unverifiable → bounded repair / re-ask → deterministic safe problem (the DTB-2 pattern, now gated on PV-1). Returns `model_used`. Tests: real-mocked + stub both return a **PV-1-verified** problem; an unverifiable proposal never escapes. | ✅ Done (this run) |
| PV-3 | **"Generate a practice problem" UI**: a prompt box → calls PV-2 → renders the verified problem in the live sandbox preview with the answer shown, honest `AIBadge`, fallback line. The on-screen wow. | ✅ Done (this run) |
| PV-4 | **Demo golden-path beat + capture**: e2e that generates → verifies → renders → (student) grades a proposed problem; screenshot/gif. | ✅ Done (this run) |

**Phase 3 DoD:** an AI-proposed practice problem is **deterministically verified
answerable** before it ships, the stub path is tested, and the propose-and-verify
beat records into the golden path. ✅ **Met** — PV-1 (the verifier keystone) +
PV-2 (the AI proposer, gated on PV-1) + PV-3 (the on-screen "generate a practice
problem" wow) + PV-4 (the demo capture: a backend-free real-browser e2e that
renders the verified problem in the sandbox, drives a student to reach exactly
the engine-verified answer, and also exercises the deterministic safe-problem
fallback — with a reproducible screenshot) shipped, end to end.

## Backlog — Demo hardening (video-first; Phases 1–3 all shipped)

With every phase's beats on screen, the routine's job shifts to keeping the
demo **film-ready**: each run re-verifies the golden path on the current tree,
fixes flakes/drift, and regenerates the reproducible artifacts.

| ID | Increment | Status |
|----|-----------|--------|
| DH-1 | **Full-demo rehearsal pass**: re-run every beat's verification (backend pytest, frontend vitest, the four real-browser e2es), fix any flake/drift found, regenerate all screenshots, and record per-beat rehearsal status in `docs/demo/golden-path.md`. | ✅ Done (2026-07-04) |
| DH-2 | **Recurring rehearsal**: repeat DH-1 whenever the tree has moved under the demo (new commits touching widgets/AI endpoints since the last pass); a no-drift pass just refreshes the rehearsal-status date. | 🔁 Standing |

## Out of scope (the "air" anti-patterns)

- **Free-form AI tutor chat.** Unbounded, ungradeable, hallucination-prone — the
  opposite of iron-clad. (There is a diverged `ai/2026-06-04-ai-tutor-chat`
  branch in the STATUS backlog; this initiative is **not** that.)
- **Arbitrary teacher code execution.** AI emits *config interpreted as data*,
  validated against a schema — never code, never `eval`/`Function`/`<script>`.
- **AI anywhere in the grading path.** Always deterministic correctness.

## Definition of Done (per increment)

- Meets the 7-point iron-clad bar above (validated · deterministic fallback +
  its test · grounded · honest `AIBadge` · demo beat · AI-out-of-grading).
- Build/lint/types/tests green (`pytest` if backend; `npm run lint && npx tsc
  --noEmit && npx vitest run` if frontend); new UI has a Vitest file.
- Ledger row appended below; the golden-path doc extended when a beat lands.

## Progress Ledger

| Date | Increment | PR | Learning |
|------|-----------|----|----------|
| 2026-07-04 | **DH-1 shipped** — the first full-demo rehearsal pass (video-first priority 2, all phase backlogs being ✅). Re-ran every beat's verification on the current tree: 169/169 backend pytest across the seven beat suites, 169/169 frontend vitest across the twelve beat suites, and 7/7 real-browser Playwright e2es — zero flakes. Found + fixed real drift: the Beat-3 (DTB-4) spec wrote its screenshot to the gitignored `e2e/__artifacts__/` dir, not `docs/demo/assets/`, so the committed `dtb4-describe-to-build.png` had been silently stale since 2026-06-21 while the doc claimed "regenerated on every run" (the playground UI had visibly changed — the stale PNG predates the PV-3 practice-problem card). One-line path fix aligns it with the other three e2es; all four assets now regenerate per run. Golden-path doc gains a **Rehearsal status** section (per-layer results + date); backlog gains DH-1/DH-2 (standing rehearsal). | _(DH-1, this run)_ | "Reproducible artifact" is only as strong as the *write path*: three of four e2es wrote straight into `docs/demo/assets/` but the first-ever capture spec (DTB-4) predated that convention and never migrated — and nothing failed, because a screenshot write can't fail an assertion. Rehearsal passes catch exactly this class of silent doc/code drift that unit CI never will; the tell was the asset's mtime (Jun 21) vs. its three siblings (regenerated same-run). |
| 2026-06-15 | Initiative promoted; Phase 1 (DTB-1..5) scoped. Owned exclusively by the ai-features routine. | _(this doc)_ | Grounded: `validate_widget_config` is floor-only today — DTB-1 (server-side per-kind schema validation) is the safety keystone the AI authoring rides on. |
| 2026-06-15 | **DTB-1 shipped** — server-side per-kind schema validation. Each kind's `params.schema.json` is **vendored** into `apps/core/data/widget_schemas/` (deploy-robust, parity-guarded against the frontend source); `validate_widget_config` now enforces the full Draft-2020-12 schema (types/bounds/enums/`required`/`additionalProperties`), rejecting bad configs with a clear 400. Guardrail keystone for DTB-2+. Golden-path Beat 0 + 16 tests (valid + invalid per kind, `{{var}}` tolerance, missing-schema floor fallback, parity). | #365 | jsonschema's bounds keywords already skip non-number instances, so a pure `{{var}}` token only trips `type`/`enum` — filtering those by `err.instance` gives clean croupier tolerance via **composition**, avoiding the deprecated validator subclassing. Vendoring (not cross-tree path reads) keeps validation working where the frontend tree is absent. |
| 2026-06-18 | **DTB-3 shipped** — the on-screen wow. `WidgetGalleryPanel`'s gallery grid now opens with a **"✨ Describe it"** prompt box: a plain-English description → `useWidgetAuthoring` POSTs `/ai/widget-authoring/` → the returned proposal drops **straight into the existing configure view** (live sandbox preview + schema form) for edit-and-attach. Honest provenance via `AIBadge`: real LLM → `✨ AI-generated`; no-key/timeout/un-salvageable → the backend's deterministic safe default arrives `ai_available:false`, shown as a neutral **`Auto-built`** badge + a friendly "AI unavailable" line. Loading/disabled/error states. Golden-path **Beat 2** + 11 frontend tests (panel: real-proposal, fallback, error, pending, disabled-empty; hook: real, stub passthrough, kind_hint, transport error). | #380 | Mocking the `useWidgetAuthoring` hook in the panel test (`vi.hoisted` + `vi.mock`) keeps the existing no-`QueryClientProvider` gallery tests green while letting new tests drive `mutate`'s `onSuccess` synchronously — no react-query plumbing in the component tests. The proposal needs **no** client-side validation because the backend contract guarantees schema-validity (Beat 0→1), so the UI just trusts it and renders into the same preview path a manual pick uses — zero parallel render code. Provenance distinction reused verbatim from DTB-2: a *clamped* config is still `✨ AI-generated`, only a *discarded→default* one is `Auto-built`. |
| 2026-06-19 | **DTB-4 shipped** — golden-path **Beat 3** (the payoff) + the first reproducibly-captured demo artifact. A backend-free Playwright e2e (`describe-to-build-grade.spec.ts`) drives the public `/widgets/dev` playground: renders the AI-describable `number-line` in the **real sandboxed iframe**, a student arrows the point to ½, and the spec asserts the host receives **0.5** across the `postMessage` boundary — the exact value the deterministic per-subpart grader scores (`AI authored, never graded`). The same run writes `docs/demo/assets/dtb4-describe-to-build.png`, so the committed screenshot can never drift from the code. Closes the type→generate→render→**grade** loop end to end (the head is the DTB-3 Vitest suites; this is the real-browser tail jsdom can't reach). | #387 | The number-line's `decimals = -floor(log10(step))` heuristic rounds **step 0.25 to one decimal**, so ¾ reports `0.8` (not 0.75) — a latent widget quirk, flagged separately; the demo uses **halves** (step 0.5 → exact 0.5) to keep the grade-signal assertion honest. Playwright drives the `allow-scripts`/no-`allow-same-origin` sandbox fine (it operates at the CDP layer, below the same-origin policy that blocks *scripted* parent access), but `.focus()` alone didn't deliver keydown to the SVG slider — a `click()` to focus, then `Home`+`ArrowRight`, is the deterministic keyboard path. |
| 2026-06-20 | **DTB-5 shipped (backend slice)** — variable-aware generation. `generate_widget_config(..., allow_variables=True)` lets the LLM bind numeric fields to croupier `{{var}}` tokens and declare `variable_constraints`; `POST /ai/widget-authoring/` gains an `allow_variables` flag and returns the validated constraints. New deterministic, LLM-free guardrail `reconcile_widget_variables` (apps/core/widgets.py): validates each range (`min<=max` real numbers, `integer` bool, `decimals` clamped 0..6), keeps only referenced+valid constraints, and **drops any `{{var}}` field with no backing constraint** so a literal token can never reach the runtime (off ⇒ all tokens stripped). Fixed the real on-screen gap: new croupier `substitute_typed` resolves a *pure* `{{token}}` leaf to the var's **native type**, so a numeric binding lands as a number (the `Number.isFinite`-guarded number-line was silently dropping the old string `"3"`). Golden-path **Beat 4** + 24 tests (reconcile keep/drop/malformed/decimals/real-randomisation, typed-vs-string substitution, generation real/strip/stub, endpoint passthrough). UI toggle deferred to **DTB-5b**. | #415 | The latent bug: IW-3b substitution always produced *strings*, but every numeric widget field is read via `Number.isFinite(cfg.x)`, which is `false` for `"3"` — so pre-DTB-5 a numeric `{{var}}` binding silently fell back to the field default and never randomised on screen. Typed-leaf resolution (pure-token ⇒ native value, mixed string ⇒ rendered string) is the fix and is backward-compatible. Keeping the token↔constraint reconciliation pure in `widgets.py` (no croupier import → no core→api layer inversion) and restricting bindings to *single-identifier* tokens makes the guardrail exact and unit-testable with zero LLM/DB. |
| 2026-06-21 | **DTB-5b shipped** — randomization, on screen. The Describe-it prompt box gains an **"Each student gets different numbers"** checkbox that sets `allow_variables` on the authoring call (`useWidgetAuthoring` now carries `allow_variables` + a typed `variable_constraints` in its response). A randomised proposal surfaces a **`🎲 Randomized per student`** pill + a line naming the bound `{{tokens}}`, and **Use this widget** forwards the server-reconciled `variable_constraints` through `onApply`. `CreateQuestionPage` merges them onto the subpart's `variable_constraints` (the field Beat 4's croupier samples) and — the real fix — makes liveness **widget-config-aware**: the new `syncVariableConstraints(text, existing, keepTokens)` keeps a constraint whose token lives in the prose *or* the attached widget config, so an AI-bound widget token is no longer silently dropped on the next text keystroke; orphans are pruned. Helpers extracted to `createQuestionConstraints.ts` (Fast-Refresh-clean, unit-tested). Golden-path **Beat 5** + 12 new frontend tests (panel: toggle off/on→flag, randomised pill+forward, non-randomised no-pill+no-forward; hook: allow_variables+constraints passthrough; helpers: keep/prune/union/widget-removal). | #424 | Following the **DTB-3 precedent**, this teacher-gallery (auth-gated) UI beat ships its proof as the Vitest suite rather than a fabricated screenshot — the reproducible demo artifact lives in Beat 3's backend-free playground e2e. The latent trap the persist had to dodge: `syncVariableConstraints` keyed liveness off the question text alone, so a widget-only `{{var}}` (never in the prose) was dropped on the very next text edit — making token-liveness the union of text ∪ widget-config tokens is the minimal correct fix, and pruning on the same union means removing the widget can never orphan a range. |
| 2026-06-22 | **GSV-1 shipped** — Phase 2 opens. Deterministic algebraic-equivalence engine `apps/core/algebra.py` (the Phase 2 correctness keystone, the counterpart to DTB-1): a safe recursive-descent parser (numbers, vars, `+ - * / ^`, unary minus, whitelisted funcs `sin`/`cos`/`ln`/`sqrt`/… — the `function-plotter` evaluator shape re-implemented on the host, **no `eval`/`exec`**) compiles each line to a pure closure, and **fixed-seed numeric probing** (no `sympy`/CAS) decides equivalence. `check_step(prev, cur)` auto-detects equation-vs-expression: equations normalise to `L - R` and match iff one is a non-zero constant multiple of the other (same solution set, so `2x = 6` ≡ `x = 3` ≡ `4x - 12 = 0`); bare lines match iff equal everywhere. Implicit multiplication (`2x`, `3(x+1)`) parses. Every malformed input is the **deterministic fallback** — `EquivalenceResult(equivalent=False, error=…)`, never a 500. **No AI yet** (correctness is, and stays, deterministic; GSV-3's AI only *explains* a wrong step). Golden-path **Beat 6** + 41 tests (equivalent/inequivalent expr & eqn across distribution/factoring/solving/identities, all malformed paths, determinism). | _(GSV-1)_ | Probing, not a CAS: with no `sympy` in the stack, seeded numeric sampling over the free vars is a reliable, reproducible equivalence oracle for the curriculum's poly/rational/transcendental forms — and it sidesteps a whole dependency. Two design seams worth recording: (1) equation-equivalence is *proportionality of the `L−R` normal forms*, which cleanly captures every legal solving move (add/sub/scale/rearrange) without ever solving; (2) mypy can't infer a `lambda` that captures via default args (the closure-per-node pattern) — `cast(_Compiled, lambda …)` keeps it typed. Multi-letter runs are one identifier (so `sin` works), an intentional limit over `xy`-style juxtaposition. |
| 2026-06-23 | **GSV-2a shipped** — the in-sandbox correctness engine (GSV-2's first slice, the keystone the widget rides on). `frontend_modern/src/widgets/step-solver/algebra.ts` is a faithful TypeScript port of the backend `apps/core/algebra.py` (GSV-1): same no-`eval`/no-`Function` recursive-descent parser, same whitelisted curriculum functions, same implicit-multiplication grammar, same **deterministic numeric-probing** equivalence (seeded → reproducible), same `checkStep`/`areExpressionsEquivalent`/`areEquationsEquivalent` API. **Why a second engine:** the widget runtime is a network-less `allow-scripts` sandbox, so the *live* per-line ✓/✗ check cannot round-trip to the backend — correctness for the live check must run in the sandbox, as deterministic JS (not AI, so principle 1's sandbox-AI-ban doesn't apply). Every malformed line is the deterministic fallback (`{equivalent:false, error:…}`, never a throw). Golden-path **Beat 6b** + 41 Vitest tests **mirroring `test_algebra.py` one-to-one** (equivalent/inequivalent expr & eqn across the curriculum forms, every malformed path, determinism) so the two engines can't silently drift. UI deferred to **GSV-2b**. | #438 | The "host calls GSV-1 per line" framing in the backlog assumed a host round-trip, but the protocol (`_sdk/protocol.ts`) is intentionally tiny — 5 message kinds, no request/response — and the sandbox is network-less by design, so a per-line backend call would require both a new protocol message *and* a network escape from a sandbox forbidden to make one. The function-plotter precedent (deterministic evaluator lives *in* the sandbox) is the right pattern: GSV-1 is deterministic, not AI, so it belongs in-sandbox for the live check. JS has no seedable RNG, so a 9-line `mulberry32` keeps probing reproducible with zero dependency; exact draws differ from Python's `random.Random` but that's irrelevant — equivalence is a property of the expressions, not the sample points. |
| 2026-06-24 | **GSV-2b shipped** — the `step-solver` widget, on screen (Phase-2 wow). A new answer-producing sandbox widget (`frontend_modern/src/widgets/step-solver/index.ts` + `params.schema.json`, registered, vendored + parity-guarded backend schema, added to `KNOWN_WIDGET_KINDS` and the per-kind valid/invalid config tests): the student solves the `prompt` equation/expression one line at a time; each line is checked **live, per keystroke** against the line above by the **inlined** Beat-6b deterministic engine (✓/✗ + a neutral reason), and the final line is reported through the existing per-subpart grader via `ctx.reportValue` — the same answer-reporting wiring as `number-line`, **AI nowhere near the grade**. Malformed lines go neutral (the deterministic fallback), never a false ✗ or a throw; no report at mount (a question can be left blank). Golden-path **Beat 7** + a Vitest suite: module surface + executed-render behaviour (✓/✗/neutral/row-growth/maxLines) **and an anti-drift battery** asserting the inlined engine's on-screen verdict matches `checkStep` from `algebra.ts` verdict-for-verdict. | _(GSV-2b)_ | The SDK serialises `render` via `Function.prototype.toString()` and bans imports/closures, and `?raw` would yield un-transpiled TS, so the GSV-2a engine **cannot** be imported into the sandbox — it must be inlined (the `function-plotter` precedent). That makes a *third* copy of the algorithm (Python · `algebra.ts` · inlined-in-render), so the load-bearing move is the parity test: drive the actual widget over a curriculum battery and compare its rendered ✓/✗ to `algebra.ts`'s `checkStep` — behavioural equivalence, not a marker grep — which is only possible because `render` is exported and happy-dom lets the test execute it with a fake `ctx`. `prompt` is left **optional** (safe default) so the existing `test_empty_config_ok_for_all_optional_kinds` stays green; `step-solver` is deliberately **not** in `AI_AUTHORABLE_WIDGET_KINDS` (Phase 2 is the deterministic-correctness widget, not an AI-authored one). |
| 2026-06-25 | **GSV-3 shipped** — the AI wrong-step explainer. `POST /api/v1/ai/step-hint/` takes two successive working lines and returns `{verdict, reason, hint, model_used, ai_available}`. The load-bearing guardrail is the **server-side gate**: the view re-runs the deterministic Beat-6 `check_step` engine and branches on *its* verdict — `correct` and `unparseable` return the engine's own reason with `hint:null` and **never call the LLM** (the tests patch `generate_step_hint` to raise if it is invoked); only a *parseable, genuinely non-equivalent* line reaches the AI, which is then **grounded** (real prev line + real wrong line + the engine's deterministic reason, instructed to nudge not re-judge). `generate_step_hint` reuses the existing Claude→Google→Ollama→stub cascade + `_call_anthropic_text`; empty LLM text falls through to the deterministic static hint, an unexpected exception degrades to it too (never a 500), and `ai_available`/`model_used` drive an honest `AIBadge`. Golden-path **Beat 8** + 11 backend tests (cascade real/empty-falls-to-stub/no-provider; endpoint wrong→AI, wrong→static fallback, correct→no-AI, unparseable→no-AI, unexpected-error→graceful, auth + 400). | _(GSV-3)_ | The "AI never grades" principle becomes a *structural* property here, not a prompt promise: correctness is decided by `check_step` **before** the model is consulted, and on two of the three branches the model is provably never reached. Reusing the text cascade verbatim (no new provider path) means the only GSV-3-specific logic is the prompt + the deterministic gate — both pure and unit-testable with a single `_call_anthropic_text` mock and no DB. The hint is coaching *text* shown to a student, not config that reaches the runtime/DB, so unlike DTB-2 it needs no schema validation — the iron-clad property it must carry instead is *out-of-the-grade-path*, which the gate enforces. UI wiring into the `step-solver` widget + capture is deferred to **GSV-4**. |
| 2026-06-27 | **GSV-4a shipped** — the AI wrong-step coach, on screen (GSV-4's first slice). `useStepHint` (react-query mutation over the GSV-3 `POST /ai/step-hint/` endpoint) + `StepHintPanel` (the host-side coach UI), wired into the `/widgets/dev` playground's **Wrong-step explainer** card, shown only for `step-solver`. The panel renders the deterministic engine's verdict (✗ / green "looks right" / neutral "couldn't check"), and — only on a genuinely `wrong` step — the LLM explanation with an honest `AIBadge`: real → `✨ AI-generated`, static-stub fallback → neutral `Auto-explanation` + an "AI unavailable" line. **AI-out-of-grade is structural**: the panel only ever renders text and never calls `reportValue`/touches the grader; the displayed verdict is always `check_step`'s, computed server-side before any model runs. Golden-path **Beat 9** + 16 frontend tests (hook: real/static-stub/correct-no-AI/transport-error; panel: ✨-badge+explanation, Auto-explanation+unavailable line, correct-no-badge, unparseable, pending/error/disabled, grounded trimmed-call; dev-page: coach shown only for step-solver with the seeded pair). | _(GSV-4a, this run)_ | The full GSV-4 ("watch the ✗, read the explanation **inline** in the widget") needs the sandbox to tell the host *which* line pair went wrong — but the protocol is intentionally 5 messages with no widget→host step channel, and the iframe is network-less, so the coach **must** be a host surface (principle 1) and the auto-feed is a new versioned protocol message — too big to bundle with the consumer. Slicing GSV-4a = the host coach + endpoint consumer (independently demoable via the playground, fully testable in jsdom) leaves GSV-4b = just the protocol wire + the real-browser capture. Following the DTB-5b precedent, this auth-gated UI slice ships its proof as the Vitest suite; the reproducible screenshot/gif rides with GSV-4b's e2e (the jsdom-unreachable tail), not a hand-captured artifact. |
| 2026-06-28 | **GSV-4b shipped** — the slip feeds the coach by itself (Phase-2 coach, loop closed). A new **additive** sandbox→host protocol message **`step`** (`protocol.ts` + `isStepMessage` guard, added to `WidgetMessage`) carries a committed line pair + the widget's **deterministic** verdict (`ok`/`bad`/`neutral`); the runtime exposes `ctx.reportStep` (`runtime.ts`/`defineWidget.ts`), the host bridge dispatches `onStep` (`host.ts`), and `InteractiveWidget` surfaces it. `step-solver` emits on **commit** (Enter/blur, deduped — never per keystroke, so the host signal is intentional), mapping its live ✓/✗ engine verdict — **never AI**. The `/widgets/dev` playground auto-feeds a `verdict:'bad'` step into the GSV-4a coach (exact pair, ⚡ "Auto-filled…" affordance; a correct step never feeds it; manual edit reverts to 'manual'). Golden-path **Beat 10** + a reproducible real-browser screenshot (`docs/demo/assets/gsv4b-step-coach-autofeed.png`). Tests: protocol guard, host dispatch, widget commit-emission (bad/ok/neutral/commit-not-keystroke/blur/empty/dedup), dev-page auto-feed (feeds-on-bad+flag / no-feed-on-correct / manual-edit-clears), and a backend-free Playwright e2e driving the real `allow-scripts` iframe → asserting the host coach received the exact wrong pair. | _(GSV-4b)_ | Adding a kind is *additive, not breaking*: the host bridge silently drops unrecognised messages, so old hosts ignore `step` and the protocol version stays `1` — the "bump on every kind" reading would have broken every existing `protocol:1` guard test for no compat benefit (all widgets rebuild from one bundle). The double-commit trap: Enter commits, then `addRow()` focuses the next row, which *blurs* the current input → a second commit of the *same* pair; a one-line `lastStepKey` dedup in the widget is cleaner than leaning on the host to ignore repeats. The backend-free e2e proves the **protocol wire** (wrong step → host receives the pair), not the AI call — the live-LLM tail needs a backend, so it stays in the mocked Vitest suites; the screenshot captures the auto-fill moment, which is the on-screen wow that doesn't need a key. |
| 2026-06-29 | **PV-1 shipped** — Phase 3 opens. Deterministic propose-and-verify problem verifier `apps/core/problem_verifier.py` (the Phase-3 correctness keystone, the counterpart to DTB-1 / GSV-1): `verify_widget_problem(kind, config, correct_answer, variable_constraints=…)` → a structured `ProblemVerdict(ok, code, reason, details)`. Confirms a proposed widget *problem* is well-posed: (1) config schema-valid (reuses DTB-1 `is_valid_widget_config`), (2) a `correct_answer` is present, (3) the answer is **reachable on the widget** — the load-bearing check. For the answer-producing `number-line` it **faithfully ports the widget's `snap`** (`decimals = max(0, -floor(log10(step)))`), so it rejects the **exact DTB-4 bug class**: ¾ on a `step 0.25` axis snaps to `0.8` and is unreachable → `code="unreachable"`, nearest reported. "Reachable" is tied to the **grader's own tolerance** (`_grade_subpart`'s 1e-3 / 1e-2), so a passing problem is one the real grader can mark. Variable-aware: samples the croupier exactly as grade-time will over a deterministic batch of synthetic students and rejects `unreachable_for_some`. Explanatory kinds (the widget doesn't bound the answer field) are a no-op `ok_explanatory`. **No AI** (correctness is, and stays, deterministic; PV-2's AI only *proposes*). Never raises — every malformed input is a deterministic `ok=False`. Golden-path **Beat 11** + 26 tests (reachable int/half-step/endpoint, unreachable ¾-step-0.25 / off-grid / out-of-range / non-numeric / bool, invalid-config / unknown-kind / missing-answer / non-dict fallbacks, explanatory no-op, variable reachable-for-all vs unreachable-for-some, determinism, unparseable expr). | _(PV-1)_ | Reachability is the Phase-3 keystone the way schema-validity was DTB-1's: an answer the *answer-producing* widget can never emit is an ill-posed problem the student is graded wrong on no matter what — so the verifier must reason about the widget's **actual** output set, which means a third faithful copy of the `number-line` snap (widget JS · here) pinned by tests against the known DTB-4 quirk. Tying "reachable" to the grader's own tolerance (not exact equality) makes the verdict *grounded in the real grade contract*, not a separate notion of "close." Lazily importing the croupier sampler/evaluator (the `apps.core.tasks` precedent) keeps the verifier in `core` without a core→api layer inversion, and sampling over fixed synthetic student ids makes the across-students well-posedness check fully deterministic and DB-free. Sliced PV-1 = the pure verifier (no endpoint) so the AI proposer (PV-2) rides on a proven guardrail, exactly as DTB-2 rode on DTB-1. |
| 2026-06-30 | **PV-2 shipped** — the AI problem proposer, gated on PV-1. `POST /api/v1/ai/practice-problem/` (teacher-only) takes an NL topic and the AI proposes a `number-line` `{widget_config, correct_answer}` via the existing Claude tool-use / Gemini-Ollama-JSON cascade; **every proposal is gated by Beat-11 `verify_widget_problem` before it can return** (the DTB-2 pattern, now on *reachability* not just schema-validity). `_finalize_problem_proposal` runs validate-config → PV-1 verify → **bounded deterministic snap-repair** (a new pure `snap_literal_answer` in `problem_verifier.py` moves an off-grid answer onto the widget's own grid, reachability-guaranteed since the snap is idempotent) → re-verify → deterministic **`SAFE_DEFAULT_PROBLEM`** — so the endpoint can never return a problem the widget can't answer (the exact DTB-4 ¾-on-step-0.25 bug class). Honest provenance via `ai_available` (real proposal vs `stub`/`safe_default`) + `repaired` + `verdict_code`; `correct_answer` rides back in the grader's own `{"answer": …}` shape. Scoped to the answer-producing `number-line` (where reachability binds) and literal answers. Golden-path **Beat 12** + 22 tests (9 pure guardrail: snap-repair the ¾/out-of-range cases, preserve reachable/extra keys, None for non-numeric/variable/missing/non-producing, safe-default verified; 6 generation: reachable passthrough, snap-repair, clamp-repair, non-numeric→default, unsalvageable→default, no-provider→default — **each asserted PV-1-valid**; 7 endpoint: success/safe-default/403/400/401/503). | _(PV-2)_ | The load-bearing move is that reachability is guaranteed **by construction, not by the prompt**: even if the model ignores the ¾-lesson in the prompt and proposes `0.75` on a `step 0.25` axis, the deterministic snap ships `0.8` (the value that axis can mark) and flags `repaired` — the guarantee survives an adversarial model because the snap reuses PV-1's own faithful port and is idempotent, so `verify(snap(x))` always passes. Keeping `snap_literal_answer` + `SAFE_DEFAULT_PROBLEM` in `problem_verifier.py` (pure, no LLM) and letting `llm_client` only orchestrate mirrors DTB-2's clean seam — the repair/fallback is unit-testable with zero LLM mock. Variable-aware *proposal* (PV-1 already verifies it) is deliberately deferred: snapping can't repair a per-student expression, so a literal-only PV-2 keeps the repair path deterministic and the PR atomic. |
| 2026-07-03 | **PV-4 shipped** — Phase 3 closed on screen. The propose-and-verify demo capture: a backend-free real-browser Playwright e2e (`practice-problem-grade.spec.ts`) drives the public `/widgets/dev` playground, **stubs the PV-2 endpoint** (via `page.route`) with the exact verified `{widget_config, correct_answer}` shape the real endpoint returns, clicks **Generate & verify**, and asserts the on-screen wow: the `✨ AI-generated` + `✓ Verified answerable` pills, the answer shown in hand, and — the payoff — the verified `number-line` rendering in the **real `allow-scripts` sandbox**, a student arrowing the point to ½, and the widget's live readout + `aria-valuenow` reporting **exactly 0.5**, the engine-verified value the deterministic grader scores (**AI proposed, engine verified, AI never graded**). A second test drives the **deterministic fallback**: a `model_used:'stub'` safe problem shows the neutral `Auto-problem` badge + "AI proposer is unavailable" line, still `✓ Verified answerable` and still interactive — never a stub-as-real. Same run writes the reproducible `docs/demo/assets/pv4-practice-problem.png`. Golden-path **Beat 14** + the 2-test e2e (real verified-render→student-reaches-answer path **and** the honest fallback path). | _(PV-4, this run)_ | The Phase-1/Phase-2 e2es (DTB-4, GSV-4b) stayed backend-free by hardcoding a config or asserting a protocol wire, never the AI call. PV-4's panel *requires* the PV-2 endpoint, so the backend-free move is `page.route` stubbing the exact verified contract PV-2 returns — the real proposer + PV-1 gate + repair/fallback are already pinned by the 22 backend pytest tests, so the e2e's job is only the browser-only tail jsdom can't reach: the verified problem rendering in the real sandbox and the student *reaching* the shown answer. Scoping the panel's sandbox iframe as `iframe[...].last().contentFrame()` (the panel renders after the main thermo-piston preview) cleanly disambiguates the two same-titled iframes without a new test hook. The on-screen proof that the shown answer is *reachable* is the number-line's own `.nl-readout strong` + `aria-valuenow` both landing on 0.5 after Home→ArrowRight — no `onValue` surfacing needed, so the teacher panel stayed untouched. |
| 2026-07-02 | **PV-3 shipped** — the AI practice-problem proposer, on screen (Phase-3 wow). The `/widgets/dev` playground gains a **"Generate a practice problem"** card (`PracticeProblemPanel` + `usePracticeProblem`): a plain-English topic → POST the PV-2 endpoint → the returned `{widget_config, correct_answer}` drops **straight into the same live sandbox preview** a hand-picked widget uses, with the **answer shown below it** and a **`✓ Verified answerable`** pill beside an honest `AIBadge`. Zero client-side re-checking — the pair is verified-answerable by construction upstream (PV-1 gates PV-2), so a ¾-topic renders the answer as **0.8** with a neutral **`Answer adjusted to the grid`** flag rather than an ungradeable 0.75. Deterministic fallback: no-key/unsalvageable shows the known-good safe problem with a neutral **`Auto-problem`** badge + "AI unavailable" line, never a stub-as-real. Structurally **out of the grade path** (host surface, renders text/preview only, never `reportValue`). Golden-path **Beat 13** + 19 frontend tests (hook: real/snap-repaired/safe-default/transport; panel: ✨+verified+preview+answer, adjusted-flag, Auto-problem+unavailable-line, pending/error/disabled/trimmed-topic-call; dev-page: always-wired). | _(PV-3, this run)_ | Following the **DTB-3 precedent**, the verified `{config, answer}` needs **no** client-side validation because the PV-2→PV-1 contract guarantees reachability, so the panel just trusts it and renders into the *same* preview path a manual pick uses — zero parallel render code, mirroring how DTB-3 trusted DTB-1/2's schema-validity. The ¾ bug becomes an on-screen *feature*: the panel surfaces the deterministically-snapped `0.8` + a `repaired` flag, making "AI proposes, the engine disposes" visible rather than hidden. As with GSV-4a, this auth/backend-gated UI slice ships its proof as the Vitest suite; the reproducible screenshot rides with PV-4's backend-free e2e (the jsdom-unreachable tail). |
| 2026-06-17 | **DTB-2 shipped** — `POST /ai/widget-authoring/` (teacher-only). NL description + the four authorable kinds' vendored schemas → LLM proposes `{widget_kind, widget_config}` via the existing Claude tool-use / Gemini-Ollama-JSON cascade. Every proposal runs the DTB-1 guardrail: **validate → deterministic clamp-repair (drop unknown keys, clamp bounds, fix enums) → safe default**, so the returned config is *always* schema-valid. Honest provenance: `ai_available`/`model_used`; the canned default reports `ai_available:false`. New widget-domain helpers in `apps/core/widgets.py` (`AI_AUTHORABLE_WIDGET_KINDS`, `SAFE_DEFAULT_CONFIGS`, `repair_widget_config`, `is_valid_widget_config`). Golden-path Beat 1 + 12 tests (real, clamp-repair, un-salvageable, no-provider — each asserted schema-valid; endpoint permission/shape). | #373 | Keeping the deterministic guardrail (defaults + clamp-repair + bool validation) in `widgets.py` and letting `llm_client` only orchestrate the LLM gives a clean seam: the repair/fallback logic is pure and unit-testable without any LLM mock, and `generate_widget_config` reuses the shared `_call_anthropic_tool` so there's no parallel provider path. Provenance honesty needs a distinction the question generator lacks — a *clamped* config is still AI (`ai_available:true, repaired:true`), but a *discarded* one that falls to the canned default is `Auto-…` (`ai_available:false`). |
