# Django Core (6.0.x — requires Python >= 3.12)
Django==6.0.7
djangorestframework==3.17.1
django-cors-headers==4.9.0
django-filter==24.3

# Database - Using psycopg (modern version, better Windows support)
psycopg[binary]==3.3.4
dj-database-url==3.1.2

# Authentication
djangorestframework-simplejwt==5.5.1

# Task Queue
celery==5.6.3
redis==8.0.1
django-celery-beat==2.9.0
django-celery-results==2.6.0

# Web Push notifications (VAPID) — mobile-native due-date reminders
pywebpush==2.3.0

# WebSockets (Real-time features)
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.2

# Static file serving in production (collectstatic + WhiteNoise middleware/storage,
# see settings/production.py — required for the prod Docker image build)
whitenoise==6.12.0

# Error tracking (OBS-3) — only active in production when SENTRY_DSN is set
# (apps/core/observability.init_sentry). The [django] extra pulls in the Django
# integration; Celery/Redis integrations ship in the base SDK.
sentry-sdk[django]==2.64.0

# Metrics exposition (OBS-6) — Prometheus client. The /metrics endpoint is
# strictly env-gated (METRICS_ENABLED); the SDK is only imported when scraped, so
# nothing runs by default in dev / test / CI.
prometheus-client==0.25.0

# Validation & Serialization
pydantic==2.13.4
pydantic-settings==2.14.2
jsonschema==4.26.0  # per-kind widget_config validation (DTB-1); also a drf-spectacular dep

# HTTP Client (for Cabinet API)
httpx==0.28.1
requests==2.34.2

# AI / LLM
anthropic==0.116.0
google-genai==2.11.0

# Utilities
python-dotenv==1.2.2
Pillow==12.3.0

# Development
django-debug-toolbar==7.0.0
django-extensions==4.1
ipython==9.15.0

# Testing
pytest==9.1.1
pytest-django==4.12.0
pytest-cov==7.1.0
factory-boy==3.3.3
faker==40.28.1

# Code Quality
black==26.5.1
flake8==7.3.0
isort==8.0.1
mypy==2.2.0
django-stubs==6.0.6
pip-audit==2.10.1

# Documentation
drf-spectacular==0.30.0
