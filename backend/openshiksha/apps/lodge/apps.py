from django.apps import AppConfig


class LodgeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "openshiksha.apps.lodge"
    verbose_name = "Lodge (Video Content)"
